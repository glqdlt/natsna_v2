<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

	<!-- a SessionFactory instance listed as /jndi/name -->
	<session-factory>
		<!-- Database connection settings -->
		<!-- <property name="connection.driver_class">org.apache.derby.jdbc.ClientDriver</property> 
			<property name="connection.url">jdbc:derby://localhost:1527/HibernateDb;create=true</property> -->
				<property name="connection.url">jdbc:mysql://localhost:3306/dls</property>
		<property name="connection.driver_class">com.mysql.jdbc.Driver</property>
		<property name="connection.username">root</property>
		<property name="connection.password">root#</property>
		<!-- <property name="dialect">org.hibernate.dialect.DerbyDialect</property> -->
		<property name="dialect">org.hibernate.dialect.MySQLDialect</property>

		<!-- JDBC connection poll -->
		<property name="connection.pool_size">1</property>
		<property name="current_session_context_class">thread</property>
		<property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>
		<property name="show_sql">true</property>
		
		<!-- 테이블이 존재할 경우의 대한 처리
		create 냐 update 냐에 따라서를 떠나서 무조건 Member 라는 테이블이 없으면 생성한다. 
		create = 있으면 drop 하고 새로 생성. 
		update = 있으면 update
		-->
		<property name="hbm2ddl.auto">update</property>
		

		<!-- entity 로 맵핑할 객체(따지고 보면 테이블) -->
		<mapping class="chap01.crud.Member" />

	</session-factory>

</hibernate-configuration>